import React from 'react';

export const metadata = {
  title: 'Statistics - AI Tool Agents',
  description: 'View detailed statistics and analytics about AI tool performance',
};

export default function StatisticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
